import { withLayout } from "layout/Layout";
import { AboutWrapper } from "./index.style";

const About = () => {
  return <AboutWrapper>AboutWrapper</AboutWrapper>;
};

export default withLayout(About);
