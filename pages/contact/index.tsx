import { withLayout } from "layout/Layout";
import { ContactWrapper } from "./index.style";

const Contact = () => {
  return <ContactWrapper>ContactWrapper</ContactWrapper>;
};

export default withLayout(Contact);
